{
  "name": "printify-mcp-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "agents": "^0.0.67",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241011.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.80.0"
  },
  "cloudflare": {
    "bindings": {
      "PRINTIFY_API_KEY": {
        "description": "Your Printify Personal Access Token (a long string starting with 'eyJ'). Generate it in Printify under Settings > Connections > Personal Access Tokens, with shops/products/uploads/orders permissions."
      }
    }
  }
}
